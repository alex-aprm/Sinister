@model Sinister.Models.Core.Dictionary

@{ViewBag.Header = "Словарь";}

@using (Html.BeginForm("Create", "Dictionaries", FormMethod.Post, new { @class = "form-horizontal", role = "form", id = "DictionaryForm" }))
{
@Html.ValidationSummary(true)
<fieldset>
<legend>Создание словаря</legend>
@Html.EditorFor(m=>m)
@Html.Partial("Actions")
</fieldset>
}
