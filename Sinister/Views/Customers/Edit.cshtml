@model Sinister.Models.CRM.Customer

@{ViewBag.Header = "Клиент";}

@using (Html.BeginForm("Edit", "Customers", FormMethod.Post, new { @class = "form-horizontal", role="form" }))
{
@Html.ValidationSummary(true)
<fieldset>
<legend>Изменение карточки клиента</legend>
@Html.EditorFor(m=>m)
@Html.Partial("Actions")
</fieldset>
}
